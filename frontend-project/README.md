# BA AI Project Frontend

Frontend riêng cho luồng đăng nhập `project` (BA/Dev dùng chung, Admin/Project Manager vẫn được phép vào).

## Run

1. Copy `.env.example` to `.env`
2. Install dependencies: `npm install`
3. Start dev server: `npm run dev`

